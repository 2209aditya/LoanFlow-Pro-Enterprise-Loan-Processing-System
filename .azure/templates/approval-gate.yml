parameters:
  environment: ''

stages:
- stage: Approval_${{ parameters.environment }}
  jobs:
  - job: Manual_Approval
    pool: server

    steps:
    - task: ManualValidation@0
      inputs:
        instructions: "Approve deployment to ${{ parameters.environment }}"
        onTimeout: reject